// Import the TodoScreen component from the components directory
import { TodoScreen } from '../../src/Components/Screens/TodoScreen';

// Export TodoScreen as the default component for this route
export default TodoScreen;